import { Text } from 'react-native';
export function CoverPicker({ value: _value, onChange: _onChange }: { value: string; onChange: (value: string) => void }) {
  return <Text>トップ画像はWeb版から変更できます</Text>;
}
